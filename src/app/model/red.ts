export class Red {

    id?: number;
    nombre: string;
    url: string;
    iconred: string;

    constructor(
        nombre: string,
        url: string,
        iconred: string
    ) {
        this.nombre = nombre;
        this.url = url;
        this.iconred = iconred;
    }
}
